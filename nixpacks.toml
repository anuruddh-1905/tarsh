providers = ["node"]

[phases.install]
cmds = ["npm ci --no-audit --audit=false"]

[phases.build]
cmds = ["npx expo export --platform web"]

[start]
cmd = "npx serve prime/dist --single --listen 8080"